name: p2p_secure_call
description: P2P Secure Video Call App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_webrtc: ^0.9.48
  web_socket_channel: ^2.4.5
  cryptography: ^2.7.0
  flutter_secure_storage: ^9.2.2
  permission_handler: ^11.3.1
  uuid: ^4.5.1
  wakelock_plus: ^1.2.8
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

